# 리서치 리포트 (내부 검토용)

아래는 이 리서치 run의 최종 마크다운 리포트 본문이다. 코드가 이 마크다운을
standalone HTML로 변환해 바탕화면에 저장한다. 커버리지 매트릭스+경고 배너는
코드가 `prepend_html`로 이 본문보다 앞에(unescaped) 직접 박으므로 여기엔 없다.

## 요청
{{REQUEST}}

## canonical seed
{{SEED_CONTRACT}}

## 회사 리서치(a)
{{STAGE_A_OUTPUT}}

## 도출(derive)
{{DERIVE_OUTPUT}}
